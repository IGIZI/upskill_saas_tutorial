<div class="home-callout text-center container-fluid">
    <h2>Welcome to DevMatch</h2>
    <h3>A membership site that i will tell you more later</h3>
</div>
    <div class="container home-signup-links">
      <div class="row">
        <% if user_signed_in? %>
        <div class="row">
          <div class="col-md-6">
            <div class="well">
              <h2 class="text-center">Your Profile</h2>
              <% if current_user.profile %>
                <%= link_to "Edit your profile", edit_user_profile_path(user_id: current_user.id) , class: 'btn btn-primary btn-lg btn-block' %>
                <%= link_to "View your profile", user_path(id: current_user.id), class: 'btn btn-primary btn-lg btn-block' %>
              <% else %>
                <p>Fill out your profile information, so that you can share your toughts with other!</p>
                <%= link_to "Create your profile", new_user_profile_path(user_id: current_user.id), class: 'btn btn-success btn-lg btn-block' if user_signed_in? %>
              <% end %>
            </div>
          </div>
          <div class="col-md-6">
            <div class="well">
              <h2 class="text-center"> The Community</h2>
              <p>Visit the community to meet other developers, entrepreneurs, and investors!</p>
              <%= link_to "Visit the community", users_path , class: 'btn btn-info btn-lg btn-block' %>
            </div>
          </div>
        
        <% else %>
        <div class="col-md-6">
          <div class="well">
            <h2 class="text-center">Basic membership</h2>
            <h4 class="text-center">Sign up for free and get Basic access</h4>
            <br/>
            <%= link_to "Sign up with basic", new_user_registration_path(plan: @basic_plan.id), class: 'btn btn-primary btn-lg btn-block' %>
          </div>
        </div>
        <div class="col-md-6">
          <div class="well">
            <h2 class="text-center">Pro membership</h2>
            <h4 class="text-center">Sign up for paid membership access</h4>
            <br/>
            <%= link_to "Sign up with pro", new_user_registration_path(plan: @pro_plan.id), class: 'btn btn-success btn-lg btn-block' %>
          </div>
        </div>
      </div>
    </div>
<% end %>